

## Goal
Make every section listed in `/admin/site-content` a fully editable CMS form that mirrors exactly what's on the live frontend — admin can add/edit/remove every field, list item, button, link, etc.

## Investigation Needed
Let me first check what `SectionEditor.tsx` currently supports vs what's missing.
